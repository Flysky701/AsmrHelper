presets:
  - id: asmr_bilingual
    label: ASMR 双语处理
    description: 对输入音频依次执行人声分离、语音识别、翻译、语音合成、混音，并导出完整结果。
    stages: [separation, asr, translation, tts, mix, export]
  - id: asr_only
    label: 仅语音识别
    description: 直接识别输入音频并导出文字稿和字幕，不执行翻译、配音或混音。
    stages: [asr, export]
