# 2026-03-27 工作日志

## 环境搭建完成

### conda环境创建
- 环境名：asmrhelper，Python 3.11
- 路径：C:\Users\Fly\.conda\envs\asmrhelper

### 安装的依赖
- torch 2.6.0+cu124（CUDA 12.4，RTX 4070 Ti SUPER 16GB）
- faster-whisper 1.2.1
- edge-tts 7.2.8
- ffmpeg-python 0.2.0
- imageio[ffmpeg]（内置ffmpeg 7.1）
- openai 2.30.0, soundfile 0.13.1, pydub, scipy, requests

### 修复的问题
1. conda-forge ffmpeg安装报GBK编码错误 → 改用 imageio_ffmpeg
2. Windows emoji导致GBK编码错误 → 改用[PASS]/[FAIL]文本
3. __init__.py被真实pyvideotrans代码覆盖 → 清空后重新创建模块文件

### 最终测试结果：10/10 全部通过

## 项目发现与使用说明整理

### 重要发现
- pyvideotrans 目录实际上是**完整的真实 pyvideotrans v3.97 项目**（285个.py文件）
- 项目自带 ffmpeg/ffmpeg-master-latest-win64-gpl/（含 ffmpeg.exe）
- 有完整 GUI（sp.py，需 PySide6）和 CLI（cli.py）入口

### 启动方式
1. GUI：`uv run sp.py`（需先安装 uv，或用 conda 补装 PySide6）
2. CLI：`uv run cli.py --task vtv --name "asmr.mp3" --source_language_code ja --target_language_code zh`
3. 自定义模块：`from videotrans.process.pipeline import run_pipeline`

### 下一步
- 可以用 uv sync 安装完整依赖后直接使用 GUI
- 也可以用已有 conda 环境 + 补装 PySide6 来启动 GUI

## PySide6 安装完成
- 版本：PySide6 6.11.0（含 PySide6_Essentials + PySide6_Addons + shiboken6）
- 安装到：C:\Users\Fly\.conda\envs\asmrhelper
- 启动 GUI 命令：`cd D:\WorkSpace\AsmrHelper\pyvideotrans && C:\Users\Fly\.conda\envs\asmrhelper\python.exe sp.py`

## 依赖安装完成（第二批/第三批）
- 第一批：flask, librosa, zhconv, jieba
- 第二批：qdarkstyle, plyer, srt, tiktoken, anthropic, deepl, websockets, tenacity
- 第三批：transformers 5.3.0, safetensors
- 所有依赖全部验证通过（17/17 OK）
